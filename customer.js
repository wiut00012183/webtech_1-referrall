const wrapperCustomer = document.querySelector(".wrapper-customer")

    function discoverPage() {
        setTimeout(() => {
            wrapperCustomer.style.opacity = "1";
        }, 1000)        
    }
    discoverPage() 